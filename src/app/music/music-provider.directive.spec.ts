import { MusicProviderDirective } from './music-provider.directive';

describe('MusicProviderDirective', () => {
  it('should create an instance', () => {
    const directive = new MusicProviderDirective();
    expect(directive).toBeTruthy();
  });
});
